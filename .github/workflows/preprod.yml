name: build

on:
  push:
    branches:
      - preprod
    paths-ignore:
      - '.ddev/**'
      - 'storage/backups/**/*'

jobs:
  backend:
    uses: ./.github/workflows/backend.yml@main
      with:
        dir: 'fantasticdevs-preprod'
      secrets: inherit
  frontend:
    needs: backend
    uses: ./.github/workflows/frontend.yml@main
      with:
        git_env: 'github-pages-preprod'
      secrets: inherit

